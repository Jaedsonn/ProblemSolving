#include <bits/stdc++.h>

using namespace std;

vector<int> dp(1e5);

int solve(vector<int>& a, int n){
    int best = 1;
    
    if(n == 0) return 1;
    int i = n;

    for(int i = n-1; a[i] >= a[n]; i--){
        
    }
}

int main(){
    vector<int> a = {10,9,2,5,3,7,101,18};
   

    return 0;
}